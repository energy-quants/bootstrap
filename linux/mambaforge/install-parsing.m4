#!/bin/bash

# m4_ignore(
echo "argbash --strip user-content 'install-parsing.m4' -o 'install-parsing.sh'" >&2
exit 11  #)
# ARG_OPTIONAL_SINGLE([version], [v], [The version of mambaforge to install.], [latest])
# ARG_DEFAULTS_POS
# ARG_HELP([Installs the specified version of mambaforge.])
# ARGBASH_GO
