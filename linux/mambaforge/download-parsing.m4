#!/bin/bash

# m4_ignore(
echo "argbash --strip user-content 'download-parsing.m4' -o 'download-parsing.sh'" >&2
exit 11  #)
# ARG_OPTIONAL_SINGLE([version], [v], [The version of mambaforge to download.], [latest])
# ARG_OPTIONAL_SINGLE([output-path], [o], [The location to save the file.], [$(pwd)])
# ARG_DEFAULTS_POS
# ARG_HELP([Downloads the specified version of mambaforge.])
# ARGBASH_GO
